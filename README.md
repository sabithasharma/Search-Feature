# Search-Feature

On searching country list data appears in a data list.
on Selecting any country and hitting enter , the selected country list along with date and 
timestamp appears below search.
on hover of the saved country list cross button appears.
On clicking of cross button the saved entry is deleted.
Nothing is saved on refresh.

Tested in Chrome and firefox browser.

Final view should look like "finalScreen.png" image
